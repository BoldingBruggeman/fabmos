# This conda environment includes everything needed to build and use FABM-OS.
name: fabmos
channels:
  - conda-forge
dependencies:
  - python>=3.8
  - cmake
  - fortran-compiler
  - pip
  - pygetm
  - scipy
  - h5py
