# This conda environment includes everything needed to build and use FABM-OS.
name: fabmos
channels:
  - conda-forge
dependencies:
  - python>=3.8
  - cmake
  - gfortran
  - pip
  - pygetm>=0.9.12
  - scipy
  - h5py
